let x = 4;
let y = 3.14;

// Define 'addition' as a function that returns the sum of 'x' and 'y'.
function addition(): number {
return x + y;
}

// Call the 'addition' function and store its return value.
let result = addition();

// If you need to output the result, for example, to the console:
console.log(result);
